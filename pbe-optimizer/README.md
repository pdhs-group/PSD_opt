# pbe-optimizer

## Module Introduction

`pbe-optimizer` is the optimization package for parameter identification in PBE simulations. It provides optimizer cores, Ray-based search workflows, and adapter-based solver integration (e.g., `dpbe` and `mcpbe`) so the same objective/data pipeline can be reused across solver backends.

