# breakage-rate-model

## Module Introduction

`breakage-rate-model` provides reusable energy-rate fitting models for breakage workflows, including power-law/separable, parametric residual-correction, and neural-network surrogates. It focuses on loading grouped HDF5 energy data, training model parameters, and returning comparable prediction/validation outputs for downstream PBE components.

